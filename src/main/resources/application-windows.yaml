spring:
  jackson:
    serialization:
      indent_output: true
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/postgres
    username: postgres
    password: topcoder872
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 30000
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: cdm
tempDir: "D:/temp/"
compileTimeout: 3000
memoryLimit: 268435456
server:
  port: 8085