components:
  schemas:
    PageInfo:
      type: object
      description: Page
      properties:
        totalElements:
          type: integer
          format: int64
          description: Total elements
        totalPages:
          type: integer
          format: integer
          description: Total pages
        number:
          type: integer
          format: integer
          description: Number
        numberOfElements:
          type: integer
          format: integer
          description: Number of elements